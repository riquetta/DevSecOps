# Terraform files
# Ignore all .terraform directories and files
**/.terraform/*
!.terraform.lock.hcl
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
*.plan
terraform.tfstate.backup

# Local variable files (if you keep sensitive data locally)
*.tfvars
*.tfvars.json

# local terraform dirs and state
infra/.terraform/
infra/terraform.tfstate
infra/terraform.tfstate.backup  
# crash log files
crash.log
# Exclude override files as they are usually used to override resources locally and should not be shared
override.tf
override.tf.json    
*_override.tf
*_override.tf.json